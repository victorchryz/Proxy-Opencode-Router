{
  "name": "nvidia-opencode-proxy",
  "version": "3.9.3",
  "description": "Clean, robust OpenCode <-> NVIDIA API proxy with cascade fallback, backoff, RPM limiter and SSE tagging.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "bun --hot index.js",
    "start": "bun index.js"
  },
  "engines": {
    "node": ">=20"
  }
}
